---

# 作業計画書（2026-01-16）: OSS公開準備（MITライセンス・.gitignore強化）

## 目的

- 本リポジトリを MIT ライセンスで公開可能な状態にする。
- 誤って秘密情報/生成物がコミットされないよう `.gitignore` を強化する。

## 参照ドキュメント（SSoT）

- `AGENTS.md`
  - 開発規則（ドキュメントSSoT、TDD、PR運用、作業計画とPR運用）
- `README.md`
  - 「重要（ライセンス/非公式）」

## 作業項目（チェックは完了時に [✓]）

- [✓] MITライセンスファイルを追加（`LICENSE`）
- [✓] `package.json` の `license` を `MIT` に変更
- [✓] `.gitignore` を強化（`.env*`, `.serena/`, `*.log`, `*.jsonl`, 鍵/証明書類, IDE設定 等）
- [✓] CI（GitHub Actions）を追加（PR の checks を報告するため）
- [✓] README に「本リポジトリのコードのライセンス」と「SCP Wiki由来コンテンツのライセンス」を分けて明記
- [✓] Prettier整形（`npm run format`）
- [✓] 検証（`npm test` / `npm run lint` / `npm run format:check`）
  - 結果: `npm test` ✅ / `npm run lint` ✅ / `npm run format:check` ✅

## 変更方針 / 注意点

- MIT ライセンスの著作権者表記は暫定で `scp-mcp contributors` とする（必要に応じて差し替え可能）。
- `.gitignore` は OSS 公開時に事故りやすいもの（秘密情報・ログ・ローカル設定・ビルド生成物）に限定して追加する。

## 影響範囲 / リスク

- 影響範囲: `LICENSE`、`package.json`、`.gitignore`、`README.md`
- リスク:
  - 著作権者表記が実態と異なるとライセンス意図とズレる可能性がある（必要なら組織名/個人名に更新する）。
